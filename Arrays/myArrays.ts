const superHeros: string[] = [];
// const heroPower: number[] = [];
const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MLModels: number[][] = [[255, 255, 255], []];

//tupla
//const tupla: [number, string] = [2, "albert"]
superHeros.push("Flash");
heroPower.push(2);

allUsers.push({ name: "albert", isActive: true });
